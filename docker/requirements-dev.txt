# Load all docker requirements
-r requirements.txt

# Install tests requirements
matplotlib>=3.6.2,<4     # TODO: verify — grep for `import matplotlib` / check .ipynb; heavy dep, remove if unused
pytest>=9,<10            # used 100% (tox runs pytest)
mongomock==4.3.0         # used 100%
pytest-cov>=7,<8         # used 100% (--cov in tox.ini)
pyflakes>=3,<4           # TODO: remove? — never invoked (no flake8/tox/CI ref), not installed
coverage>=7,<8           # TODO: remove? — transitive via pytest-cov, not installed standalone
tox>=4,<5                # TODO: remove? — CI installs tox separately; circular here
